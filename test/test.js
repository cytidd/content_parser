'use strict';

var expect = require('chai').expect;
var parser = require('../index');

describe('#index', function() {
    it('should parse CNN', function() {
        expect(1).to.equals(1);
    });
});

